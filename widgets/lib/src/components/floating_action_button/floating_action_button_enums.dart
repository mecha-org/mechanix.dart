/// Available size variants for [MechanixFloatingActionButton].
enum MechanixFloatingActionButtonSize {
  /// Small FAB (56x56 px, default).
  small,

  /// Medium FAB (80x80 px).
  medium,

  /// Large FAB (96x96 px).
  large,
}
